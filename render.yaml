services:
  - type: web
    name: llama3-fact-checker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run your_script_name.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: BRAVE_API_KEY
        sync: false